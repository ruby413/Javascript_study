const solution = (numbers, target) => {
    let stack = [];
    let resultNumber = 0;
    let answer = 0;
    
    numbers.forEach((el)=>{
        resultNumber + = el;
    })
    if(resultNumber === target){
        answer + = 1;
    }
}

// try again!
